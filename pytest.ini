# pytest 配置：默认排除 integration 集成测试（需真实外部服务）。
# 显式运行集成测试：pytest -m integration（命令行 -m 覆盖 addopts）
[pytest]
markers =
    integration: 需要真实外部服务（如 OpenSearch @192.168.9.12）的集成测试，默认跳过
addopts = -m "not integration"
